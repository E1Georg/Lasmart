class UpdatePointDto {
    ID
    x
    y
    Radius
    Color

    constructor(ID, x, y, Radius, Color) {
        this.ID = ID;   
        this.x = x;
        this.y = y;
        this.Radius = Radius;
        this.Color = Color;
    }    
}