class CreatePointDto {
    x
    y
    Radius
    Color

    constructor(x, y, Radius, Color) {       
        this.x = x;
        this.y = y;
        this.Radius = Radius;
        this.Color = Color;
    }    
}